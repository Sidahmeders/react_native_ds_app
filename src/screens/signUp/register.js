import React, { useState } from 'react';
import { View, Text } from 'react-native';



function Register() {

  return (
    <View style={{flex:1, justifyContent:"center", alignItems:"center"}}>
        <Text style={{fontSize:30, color:"#29d"}}>Register Screen.</Text>
    </View>
  );
};

export default Register;
